%{
#include <stdio.h> //SOUMYA BAJIRAO
int count = 0;
%}

%%
^[Aa][a-zA-Z]*   { printf("Matched: %s\n", yytext); count++; }

.|\n             ;
%%

int main()
{
    yylex();
    printf("Number of words starting with 'A' or 'a': %d\n", count);
    return 0;
}

int yywrap()
{
    return 1;
}
